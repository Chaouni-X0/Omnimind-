import { Router, Request, Response } from 'express';\nimport { terminalService } from '../services/terminalService';\n\nconst router = Router();\n\n/**\n * Create a new terminal session\n * POST /api/terminal/sessions\n */\nrouter.post('/sessions', (req: Request, res: Response) => {\n  try {\n    const { cwd } = req.body;\n    const sessionId = terminalService.createSession(cwd || process.cwd());\n\n    res.json({\n      success: true,\n      sessionId,\n      message: 'Terminal session created',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Execute a command in a terminal session\n * POST /api/terminal/execute\n */\nrouter.post('/execute', async (req: Request, res: Response) => {\n  try {\n    const { sessionId, command } = req.body;\n\n    if (!sessionId || !command) {\n      return res.status(400).json({\n        success: false,\n        error: 'sessionId and command are required',\n      });\n    }\n\n    const result = await terminalService.executeCommand(sessionId, command);\n\n    res.json({\n      success: result.success,\n      output: result.output,\n      error: result.error,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Get terminal session output\n * GET /api/terminal/output/:sessionId\n */\nrouter.get('/output/:sessionId', (req: Request, res: Response) => {\n  try {\n    const { sessionId } = req.params;\n    const output = terminalService.getOutput(sessionId);\n\n    res.json({\n      success: true,\n      output,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Clear terminal session output\n * DELETE /api/terminal/output/:sessionId\n */\nrouter.delete('/output/:sessionId', (req: Request, res: Response) => {\n  try {\n    const { sessionId } = req.params;\n    terminalService.clearOutput(sessionId);\n\n    res.json({\n      success: true,\n      message: 'Terminal output cleared',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Close a terminal session\n * DELETE /api/terminal/sessions/:sessionId\n */\nrouter.delete('/sessions/:sessionId', (req: Request, res: Response) => {\n  try {\n    const { sessionId } = req.params;\n    terminalService.closeSession(sessionId);\n\n    res.json({\n      success: true,\n      message: 'Terminal session closed',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Get all active terminal sessions\n * GET /api/terminal/sessions\n */\nrouter.get('/sessions', (req: Request, res: Response) => {\n  try {\n    const sessions = terminalService.getActiveSessions();\n\n    res.json({\n      success: true,\n      sessions,\n      count: sessions.length,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\nexport default router;\n
