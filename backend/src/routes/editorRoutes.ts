import { Router, Request, Response } from 'express';\nimport { editorService } from '../services/editorService';\n\nconst router = Router();\n\n/**\n * Initialize storage\n * POST /api/editor/init\n */\nrouter.post('/init', async (req: Request, res: Response) => {\n  try {\n    await editorService.initializeStorage();\n\n    res.json({\n      success: true,\n      message: 'Storage initialized',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * List files in a directory\n * GET /api/editor/files?path=<dirPath>\n */\nrouter.get('/files', async (req: Request, res: Response) => {\n  try {\n    const { path } = req.query;\n    const files = await editorService.listFiles((path as string) || '');\n\n    res.json({\n      success: true,\n      files,\n      count: files.length,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Read file content\n * GET /api/editor/read?path=<filePath>\n */\nrouter.get('/read', async (req: Request, res: Response) => {\n  try {\n    const { path } = req.query;\n\n    if (!path) {\n      return res.status(400).json({\n        success: false,\n        error: 'path is required',\n      });\n    }\n\n    const file = await editorService.readFile(path as string);\n\n    if (!file) {\n      return res.status(404).json({\n        success: false,\n        error: 'File not found',\n      });\n    }\n\n    res.json({\n      success: true,\n      file,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Write file content\n * POST /api/editor/write\n */\nrouter.post('/write', async (req: Request, res: Response) => {\n  try {\n    const { path, content } = req.body;\n\n    if (!path || content === undefined) {\n      return res.status(400).json({\n        success: false,\n        error: 'path and content are required',\n      });\n    }\n\n    const success = await editorService.writeFile(path, content);\n\n    res.json({\n      success,\n      message: success ? 'File written' : 'Failed to write file',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Create a new file\n * POST /api/editor/create\n */\nrouter.post('/create', async (req: Request, res: Response) => {\n  try {\n    const { path, content } = req.body;\n\n    if (!path) {\n      return res.status(400).json({\n        success: false,\n        error: 'path is required',\n      });\n    }\n\n    const success = await editorService.createFile(path, content || '');\n\n    res.json({\n      success,\n      message: success ? 'File created' : 'File already exists',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Delete a file\n * DELETE /api/editor/delete?path=<filePath>\n */\nrouter.delete('/delete', async (req: Request, res: Response) => {\n  try {\n    const { path } = req.query;\n\n    if (!path) {\n      return res.status(400).json({\n        success: false,\n        error: 'path is required',\n      });\n    }\n\n    const success = await editorService.deleteFile(path as string);\n\n    res.json({\n      success,\n      message: success ? 'File deleted' : 'Failed to delete file',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Create a new directory\n * POST /api/editor/mkdir\n */\nrouter.post('/mkdir', async (req: Request, res: Response) => {\n  try {\n    const { path } = req.body;\n\n    if (!path) {\n      return res.status(400).json({\n        success: false,\n        error: 'path is required',\n      });\n    }\n\n    const success = await editorService.createDirectory(path);\n\n    res.json({\n      success,\n      message: success ? 'Directory created' : 'Failed to create directory',\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\n/**\n * Get syntax highlighting info for a file\n * GET /api/editor/syntax?path=<filePath>\n */\nrouter.get('/syntax', (req: Request, res: Response) => {\n  try {\n    const { path } = req.query;\n\n    if (!path) {\n      return res.status(400).json({\n        success: false,\n        error: 'path is required',\n      });\n    }\n\n    const language = editorService.getSyntaxHighlighting(path as string);\n    const isSupported = editorService.isSupportedFile(path as string);\n\n    res.json({\n      success: true,\n      language,\n      isSupported,\n    });\n  } catch (error: any) {\n    res.status(500).json({\n      success: false,\n      error: error.message,\n    });\n  }\n});\n\nexport default router;\n
